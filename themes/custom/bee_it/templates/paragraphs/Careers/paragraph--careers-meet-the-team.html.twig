<section class="b-promotion b-promotion--careers">
  <div class="l-container">
    <hr class="orange">
    <h2 class="b-promotion__title h2">{{ content.field_title }}</h2>
    <p class="b-promotion__sub-title">{{ content.field_subtitle }}</p>
    <p class="b-promotion__text">{{ content.field_description }}</p>
    <a href="{{ content.field_link.0['#url'] }}" class="l-btn l-btn--green" >{{ content.field_link.0['#title'] }}</a>
  </div>
</section>